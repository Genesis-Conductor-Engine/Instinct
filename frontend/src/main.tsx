import React from 'react';
import ReactDOM from 'react-dom/client';
import BlackhorseKGraphDashboard from './BlackhorseKGraph';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BlackhorseKGraphDashboard />
  </React.StrictMode>
);
